{
  "name": "@kurdel/sample-auth",
  "version": "0.1.0-beta.6",
  "private": true,
  "description": "example app with auth",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "test": "vitest run",
    "test:ui": "vitest",
    "test:watch": "vitest --watch",
    "coverage": "vitest run --coverage",
    "clean": "rimraf dist ../../.cache/tsconfig.sample.auth.build.tsbuildinfo",
    "build": "tsc",
    "build:force": "npm run clean && tsc --force",
    "dev": "node dist/app.js"
  },
  "author": "Andrii Sorokin",
  "license": "MIT",
  "dependencies": {
    "@kurdel/auth": "0.1.0-beta.6",
    "@kurdel/core": "0.1.0-beta.6",
    "@kurdel/facade": "0.1.0-beta.6",
    "@kurdel/ioc": "0.1.0-beta.6"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "@types/supertest": "^6.0.3",
    "@vitest/coverage-v8": "^3.2.4",
    "rimraf": "^6.0.1",
    "supertest": "^7.1.4"
  }
}
