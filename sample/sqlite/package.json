{
  "name": "@kurdel/sample-sqlite",
  "version": "0.1.0-beta.6",
  "private": true,
  "description": "example simple app with sqlite DB",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "test": "vitest run",
    "test:ui": "vitest",
    "test:watch": "vitest --watch",
    "coverage": "vitest run --coverage",
    "clean": "rimraf dist ../../.cache/tsconfig.sample.sqlite.build.tsbuildinfo",
    "build": "tsc",
    "build:force": "npm run clean && tsc --force",
    "dev": "node dist/app.js"
  },
  "author": "Andrii Sorokin",
  "license": "MIT",
  "dependencies": {
    "@kurdel/core": "0.1.0-beta.6",
    "@kurdel/facade": "0.1.0-beta.6",
    "sqlite3": "^6.0.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@kurdel/migrations": "0.1.0-beta.6",
    "@types/node": "^20.9.0",
    "@types/supertest": "^6.0.3",
    "@vitest/coverage-v8": "^3.2.4",
    "rimraf": "^6.0.1",
    "supertest": "^7.1.4"
  }
}
