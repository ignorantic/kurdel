import React from 'react';
import { MigrationManager } from '@kurdel/core';
type Props = {
    manager: MigrationManager;
};
export default function MigrateRollback({ manager }: Props): React.JSX.Element;
export {};
