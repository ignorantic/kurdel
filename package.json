{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "sample/*"
  ],
  "scripts": {
    "test": "npm run test --ws",
    "clean": "npm run clean --workspaces",
    "clean:all": "npm run clean --workspaces && rimraf .cache",
    "build": "npm run build --ws",
    "rebuild": "npm run clean:all && npm run build"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "supertest": "^7.1.4",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
