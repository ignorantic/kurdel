{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "sample/*"
  ],
  "scripts": {
    "build": "npm run build --ws"
  },
  "dependencies": {
    "@types/node": "^20.12.2"
  }
}
